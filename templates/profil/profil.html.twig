{% extends 'base.html.twig' %}
{% block title %}Edit profil{% endblock %}

{% block body %}
    {% if error %}
        <center>
            <div class="alert alert-danger">{{ error }}</div>
        </center>
    {% endif %}
    <div>
        <center><h1 class="alert-link"> My profil ! </h1></center>
    </div>
    {% if user.photo %}
        <center>
            <img src="{{ asset('img/images/' ~ user.photo) }}">
        </center>
    {% endif %}
    {% if user.photo == false %}
        <center><div> Vous n'avez pas encore de photo photo de profil</div></center>
    {% endif %}
    <div class="form">

        <div class="mt-4">
            {{ form_start(userForm) }}

            {{ form_widget(userForm) }}
            
        </div>
    </div>
    <div class="col-12 col-sm-6 col-md-7 offset-4">
        <button class="btn btn-lg btn-primary" type="submit">
            Enregister
        </button>

        <a class="btn btn-lg btn-danger"
           href="{{ path('main_home') }}">Retourner à
            la page d'accueil</a>
    </div>

    {{ form_end(userForm) }}

{% endblock %}

{% block style %}
    <style type="text/css">
        img {
            width:150px;
            height:150px;
        }
    </style>
{% endblock %}