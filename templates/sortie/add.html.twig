{% extends 'base.html.twig' %}

{% block title %}Créer une sortie{% endblock %}


{% block body %}

    <h1>Proposer une sortie</h1>

<div class="form">
    {{ form_start(sortieForm) }}

    {{ form_widget(sortieForm) }}

    <!--<button type="submit" class="btn" name="save" id="saveBtnForNewSortie">Enregistrer</button>
    <button type="submit" class="btn" name="submit" id="submitBtnForNewSortie">Publier la sortie</button>--!>
    <button class="btn"><a href="{{ path('sortie_lieu') }}">Ajouter un lieu</a></button>
    <button class="btn"><a href="{{ path('main_accueil') }}">Annuler</a></button>
    {{ form_end(sortieForm) }}
</div>


{% endblock %}
{% block javascripts %}
    <script src="{{ asset('js/addSortie.js') }}"></script>
{% endblock %}
